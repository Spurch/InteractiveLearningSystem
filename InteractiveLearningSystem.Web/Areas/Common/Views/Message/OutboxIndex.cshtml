@model IEnumerable<InteractiveLearningSystem.Models.Message>

@{
	ViewBag.Title = "Messages";
}

<div class="well text-center">
	<h2>Messages</h2>
</div>

<div class="well">
	<table class="table table-responsive">
		<tr>
			<th>
				@Html.DisplayNameFor(model => model.Receiver)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.Title)
			</th>
			<th>
				@Html.DisplayNameFor(model => model.DateCreated)
			</th>
			<th class="pull-right">Action</th>
		</tr>

		@foreach (var item in Model)
		{
			<tr>
				<td>
					@Html.ActionLink(@Html.DisplayFor(modelItem => item.Receiver.FirstName).ToString(), "Details", "User", new { id = item.ReceiverId }, "")
				</td>
				<td>
					@Html.ActionLink(@Html.DisplayFor(modelItem => item.Title).ToString(), "Details", new { id = item.Id })
				</td>
				<td>
					@Html.DisplayFor(modelItem => item.DateCreated)
				</td>
				<td class="pull-right">
					<a href="@Url.Action("Details", "Message", new { id=item.Id })" class="btn btn-success">Details</a>
				</td>
			</tr>
		}

	</table>
</div>

